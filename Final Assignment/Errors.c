//PROGRAM TO IDENTIFY ERRORS 	NAME HAMZA RIAZ KHAN 	ROLLNO CR-031

//All Commented Lines Are The Correct Codes

#include<stdio.h>
int main ()
{
	scanf( "d", value );
	//scanf( "%d", value );
	
	printf( "The product of %d and %d is %d"\n, x, y );
	//printf( "The product of %d and %d is %d\n", x, y,z );
	
	firstNumber + secondNumber = sumOfNumbers
	//sumOfNumbers =firstNumber + secondNumber 
	
	if ( number => largest )
	largest == number;
	//if ( number >= largest )
	//	largest == number;
	
	*/ Program to determine the largest of three integers /*
	/* Program to determine the largest of three integers */
	
	Scanf( "%d", anInteger );
	//scanf( "%d", anInteger );
	
	printf( "Remainder of %d divided by %d is\n", x, y, x % y );
	//printf( "Remainder of %d divided by %d is %d\n", x, y, x % y );
	
	if ( x = y );
	printf( %d is equal to %d\n", x, y );
	//if ( x = y )
	//	printf( "%d is equal to %d\n", x, y );
	
	print( "The sum is %d\n," x + y );
	//printf( "The sum is %d\n", x + y );
	
	Printf( "The value you entered is: %d\n, &value );
	//printf( "The value you entered is: %d\n", value );
	
	int x = 1, total;
	while ( x <= 10 ) {
	total += x;
	++x; }
	//int x = 1, total;
	//while ( x <= 10 ) {
	//total = x;
	//++x; 

	While ( x <= 100 )
	total += x;
	++x;
	//while ( x <= 100 )
	//total = x;
	//++x;

	while ( y > 0 ) {
	printf( "%d\n", y );
	++y; }
	//Already Correct

	For ( x = 100, x >= 1, ++x )
	printf( "%d\n", x );
	//for ( x = 100; x >= 1; ++x )
	//printf( "%d\n", x );

	The following code should print whether a given integer is odd or even:
	switch ( value % 2 ) {
	case 0:
	puts( "Even integer" );
	case 1:
	puts( "Odd integer" ); }
	//switch ( value % 2 ) {
	//case 0:
	//puts( "Even integer" );
	//	break;
	//default:
	//puts( "Odd integer" ); }
	
	The following code should input an integer and a character and print them. Assume the
	user types as input 100 A.
	scanf( "%d", &intVal );
	charVal = getchar();
	printf( "Integer: %d\nCharacter: %c\n", intVal, charVal );
	//scanf( "%d", &intVal );
	//getchar();
	//scanf( "%d", &charVal );
	//printf( "Integer: %d\nCharacter: %c\n", intVal, charVal );
	
	for ( x = .000001; x == .0001; x += .000001 ) {
	printf( "%.7f\n", x ); }
	//for ( x = .000001; x <= .0001; x += .000001 ) {
	//printf( "%.7f\n", x ); }
	
	
	The following code should output the odd integers from 999 to 1:
	for ( x = 999; x >= 1; x += 2 ) {
	printf( "%d\n", x ); }
	//for ( x = 1; x <= 999; x+=2 ) {
	//printf( "%d\n", x ); }

	The following code should output the even integers from 2 to 100:
	counter = 2;
	Do {
	if ( counter % 2 == 0 ) {
	printf( "%u\n", counter ); }
	counter += 2;
	} While ( counter < 100 );
	//int counter = 2;
	//do {
	//if ( counter % 2 == 0 ) {
	//printf( "%u\n", counter ); }
	//counter += 2;
	//} while ( counter < 100 );
	
	The following code should sum the integers from 100 to 150 (assume total is initialized to 0):
	for ( x = 100; x <= 150; ++x ); {
	total += x;
	}
	//for ( x = 100; x <= 150; ++x ) {
	//total += x;
	//}
	//printf("%d",total);
	}
